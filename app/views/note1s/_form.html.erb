<%= form_with(model: note1) do |form| %>
  <% if note1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(note1.errors.count, "error") %> prohibited this note1 from being saved:</h2>

      <ul>
        <% note1.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.number_field :age %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
